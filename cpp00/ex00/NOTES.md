20/02
There are operands and operators in C++;
namespace is like collection of names that represents the standard library in C++;
<< is a operator that acts if it is a function

22/02
Operador de escopo -> ::
É usado para definir em qual escopo (parte esquerda) o name (parte direita) está
ex.: std::cout
o name cout que está no escopo std (namespace)

the interface is implicit inside a object type; **interface** is a collection of
operations that you can perform on a object of that type;
ex.: std::string name;
we are able to do whatever the library says that we can do with string, like name.length();

OOP
Class -> It's a model or a template;
Object -> It's a instance or a example of that Class;
Classes have attributes (characteristics) and behaviour;

